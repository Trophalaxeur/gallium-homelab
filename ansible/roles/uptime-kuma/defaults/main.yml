---
uptime_kuma_install_dir: /opt/uptime-kuma
# Host port Kuma listens on. Backup jobs push over plain HTTP to this port
# (http://uptime.lan:3001); the human dashboard is fronted by Caddy with a
# Let's Encrypt cert (uptime-cert role), same pattern as immich.
uptime_kuma_port: 3001
