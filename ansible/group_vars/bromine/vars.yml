---
# Vault vars required (add via `ansible-vault edit group_vars/all/vault.yml`):
#   vault_smtp_password           — SMTP App Password (Gmail — me@flefevre.fr)
#   vault_gh_admin_token          — GitHub PAT with Administration:read+write on
#                                    carbon-notes + bismuth-blog (deploy key registration)
#   vault_anthropic_api_token     — Anthropic API key (production LLM calls)
#   vault_google_client_id        — Google OAuth 2.0 client ID (bromine-cv-extension auth)
#   vault_google_client_secret    — Google OAuth 2.0 client secret
#   vault_bromine_allowed_emails  — comma-separated allowlist of emails permitted to authenticate
